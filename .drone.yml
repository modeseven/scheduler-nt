kind: pipeline
type: kubernetes
name: default

steps:
  - name: build
    image: adoptopenjdk/openjdk11
    user: root
    commands:
      - echo hello
      - echo world
      - ./mvnw clean package -Dquarkus.kubernetes.deploy=true
