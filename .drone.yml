kind: pipeline
type: kubernetes
name: default

steps:
  - name: build
    image: adoptopenjdk/openjdk11
    commands:
      - echo hello
      - echo world
      - ./mvnw clean package -Dquarkus.kubernetes.deploy=true
